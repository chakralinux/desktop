
pkgname=qmidictl
pkgver=0.2.0
pkgrel=1
pkgdesc="A MIDI remote controller application that sends MIDI data over the network, using UDP/IP multicast."
arch=('x86_64')
url="http://qmidictl.sourceforge.net/"
license=('GPL')
depends=('qt' 'hicolor-icon-theme')
categories=('multimedia' 'network')
install="$pkgname.install"
source=("http://downloads.sourceforge.net/project/$pkgname/$pkgname/$pkgver/$pkgname-$pkgver.tar.gz")
md5sums=('51d10a01a3930d67c45fa9880e30bd24')

build() {
  cd $pkgname-$pkgver

  ./configure --prefix=/usr
  make
}

package() {
  cd $pkgname-$pkgver

  make DESTDIR=$pkgdir install
}
