post_install() {
  echo "==> "
  echo "==> To enable systemd-swap, run 'sudo systemctl enable systemd-swap'"
  echo "==> To run the script now, start it with 'sudo systemctl start systemd-swap'"
  echo "==> Config in /etc/systemd-swap.conf"
  echo "==> "
}
