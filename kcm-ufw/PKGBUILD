#
# Apps Packages for Chakra, part of chakra-project.org
#
# maintainer (i686): Phil Miller <philm[at]chakra-project[dog]org>
# maintainer (x86_64): Manuel Tortosa <manutortosa[at]chakra-project[dot]org>

pkgname=kcm-ufw
pkgver=0.0.3
pkgrel=1
pkgdesc="UFW KControl Module"
arch=('i686' 'x86_64')
url="http://kde-apps.org/content/show.php/UFW+KControl+Module?content=137789"
license=('GPL')
depends=('ufw' 'kdebase-workspace' 'perl' 'openssl' 'polkit-kde-agent')
makedepends=('automoc4' 'cmake')
source=(http://kde-apps.org/CONTENT/content-files/137789-kcm_ufw-$pkgver.tar.bz2)
md5sums=('1652e55ab590303c989e5b166ecc8879')

build() {
  cd "$srcdir/kcm_ufw-$pkgver/"
  cmake . -DCMAKE_INSTALL_PREFIX=/usr
  make || return 1
}

package() {
  cd "$srcdir/kcm_ufw-$pkgver"
  # hacks to get it into pkg-dir
  sed -i -e 's~"/usr~"/home/phil/apps/kcm-ufw/pkg/usr~g' cmake_install.cmake
  sed -i -e 's~"/usr~"/home/phil/apps/kcm-ufw/pkg/usr~g' helper/cmake_install.cmake
  sed -i -e 's~"/etc~"/home/phil/apps/kcm-ufw/pkg/etc~g' helper/cmake_install.cmake
  sed -i -e 's~"/usr~"/home/phil/apps/kcm-ufw/pkg/usr~g' kcm/cmake_install.cmake
  sed -i -e 's~"/usr~"/home/phil/apps/kcm-ufw/pkg/usr~g' po/cmake_install.cmake
  make install || return 1
}
